@page "{id:int}"
@model NMC.Pages.RoomTypes.EditRoomTypeModel
@inject IViewLocalizer _localizer
@{
    ViewData["PageTitle"] = _localizer["Edit Room Type"];
}

<div class="content">
    <div class="row">
        <div class="col-lg-8 offset-lg-2">
            <h4 class="page-title">@_localizer["Edit Room Type"]</h4>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-8 offset-lg-2">
            <form method="post">
                <input asp-for="RoomType.Id" type="hidden" />
                <div class="form-group">
                    <label asp-for="RoomType.Name"></label>
                    <input asp-for="RoomType.Name" class="form-control">
                </div>
                <div class="form-group">
                    <label asp-for="RoomType.NameAr"></label>
                    <input asp-for="RoomType.NameAr" class="form-control">
                </div>
                <div class="m-t-20 text-center">
                    <button class="btn btn-primary submit-btn">@_localizer["Save"]</button>
                </div>
            </form>
        </div>
    </div>
</div>