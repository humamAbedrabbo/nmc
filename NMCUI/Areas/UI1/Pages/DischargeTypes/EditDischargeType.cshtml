@page "{id:int}"
@model NMCUI.Areas.UI1.Pages.DischargeTypes.EditDischargeTypeModel
@inject IViewLocalizer _localizer
@{
    ViewData["PageTitle"] = _localizer["Edit Discharge Type"];
}

<div class="content">
    <div class="row">
        <div class="col-lg-8 offset-lg-2">
            <h4 class="page-title">@_localizer["Edit Discharge Type"]</h4>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-8 offset-lg-2">
            <form method="post">
                <input asp-for="DischargeType.Id" type="hidden" />
                <div class="form-group">
                    <label asp-for="DischargeType.Name"></label>
                    <input asp-for="DischargeType.Name" class="form-control">
                </div>
                <div class="form-group">
                    <label asp-for="DischargeType.NameAr"></label>
                    <input asp-for="DischargeType.NameAr" class="form-control">
                </div>
                <div class="m-t-20 text-center">
                    <button class="btn btn-primary submit-btn">@_localizer["Save"]</button>
                </div>
            </form>
        </div>
    </div>
</div>